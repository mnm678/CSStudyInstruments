There were a few projects I explored this week that have not yet yielded any bugs:

* I spent some time trying to get scrapy to run in a single process so that it can work with crash crash simulator.
*
